import React from 'react';
import styles from '../styles/Form.module.css';

const Form = ({ form }) => {
    return (
        <section className="form-control">
            { form }
        </section>
)
};
export default Form;